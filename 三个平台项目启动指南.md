# IPFS Desktop Rust：Windows、macOS、Linux 启动指南

目标分支：`prod`

> `npm install` 或 `npm ci` 只能安装 JavaScript/前端依赖，不能代替 Rust、系统编译工具和 Tauri 平台依赖。新电脑建议使用 `npm ci`，以严格按照仓库中的 `package-lock.json` 复现依赖。

## Windows 10/11

### 1. 安装基础环境

以管理员身份打开 PowerShell：

```powershell
winget install --id Git.Git -e
winget install --id OpenJS.NodeJS.LTS -e
winget install --id Rustlang.Rustup -e
winget install --id Microsoft.VisualStudio.2022.BuildTools -e --override "--wait --passive --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
```

Windows 10/11 通常已经包含 Edge WebView2。若系统没有，请执行：

```powershell
winget install --id Microsoft.EdgeWebView2Runtime -e
```

安装结束后关闭并重新打开 PowerShell，然后配置 Rust：

```powershell
rustup default stable-msvc
```

### 2. 克隆项目并安装依赖

```powershell
git clone -b prod --single-branch https://github.com/xuanxi369/IPFS-Desktop-Rust.git
cd IPFS-Desktop-Rust
npm ci
```

### 3. 准备 Kubo sidecar

```powershell
npm run setup:kubo
```

### 4. 启动或构建

开发模式启动桌面应用：

```powershell
npm run tauri dev
```

构建正式程序及安装包：

```powershell
npm run tauri build
```

构建结果通常位于：

```text
src-tauri\target\release\bundle\
```

如果需要构建 MSI，请在 Windows“可选功能”中启用 `VBSCRIPT`。

## macOS

### 1. 安装基础环境

安装 Xcode 命令行工具：

```bash
xcode-select --install
```

如果尚未安装 Homebrew：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

安装 Git 和 Node.js 20：

```bash
brew install git node@20
brew link --overwrite node@20
```

安装 Rust stable：

```bash
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
source "$HOME/.cargo/env"
rustup default stable
```

### 2. 克隆项目并启动

```bash
git clone -b prod --single-branch https://github.com/xuanxi369/IPFS-Desktop-Rust.git
cd IPFS-Desktop-Rust
npm ci
npm run tauri dev
```

构建 `.app`/`.dmg`：

```bash
npm run tauri build
```

### 3. Kubo 说明

项目的 `npm run setup:kubo` 是 Windows PowerShell 脚本，不能直接用于 macOS。需要 Kubo 兼容功能时，请单独安装 Kubo，确保 `ipfs` 位于 `PATH`，或者设置：

```bash
export IPFS_GO_EXEC="/实际路径/ipfs"
```

## Linux（Ubuntu/Debian）

建议使用 Ubuntu 22.04/24.04 或 Debian 12。

### 1. 安装系统依赖

```bash
sudo apt update
sudo apt install -y \
  git curl wget file build-essential \
  libwebkit2gtk-4.1-dev \
  libayatana-appindicator3-dev \
  librsvg2-dev \
  libxdo-dev \
  libssl-dev \
  patchelf \
  libgtk-3-dev \
  libjavascriptcoregtk-4.1-dev \
  libsoup-3.0-dev \
  libdbus-1-dev \
  libsecret-1-dev
```

### 2. 安装 Node.js 20

使用 `nvm`：

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
source "$HOME/.bashrc"
nvm install 20
nvm use 20
```

### 3. 安装 Rust stable

```bash
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
source "$HOME/.cargo/env"
rustup default stable
```

### 4. 克隆项目并启动

```bash
git clone -b prod --single-branch https://github.com/xuanxi369/IPFS-Desktop-Rust.git
cd IPFS-Desktop-Rust
npm ci
npm run tauri dev
```

构建 Linux 安装包：

```bash
npm run tauri build
```

### 5. Kubo 说明

Linux 不能运行 Windows 专用的 `npm run setup:kubo`。需要 Kubo 时，请单独安装官方 Kubo，并确保：

```bash
ipfs version
```

能够正常返回版本；也可以设置：

```bash
export IPFS_GO_EXEC="/实际路径/ipfs"
```

## 三个平台通用验证命令

进入项目目录后执行：

```bash
node --version
npm --version
rustc --version
cargo --version
git --version

npm run typecheck
npm test
cargo check --manifest-path src-tauri/Cargo.toml
```

启动桌面应用：

```bash
npm run tauri dev
```

只启动前端网页（不会启动完整的 Rust/Tauri 后端）：

```bash
npm run dev
```

## 最短流程总结

环境安装完成后，各系统的项目操作基本一致：

```bash
git clone -b prod --single-branch https://github.com/xuanxi369/IPFS-Desktop-Rust.git
cd IPFS-Desktop-Rust
npm ci
npm run tauri dev
```

但必须先安装对应系统的编译环境、Node.js 20 和 Rust stable。
