# IPFS Desktop Rust：Windows、macOS、Linux 与 VPS 启动指南

本文适用于当前 `IPFS-Desktop-Rust` 项目。项目位于同一个仓库中，但提供三种运行入口：

- 桌面版：`ipfs-desktop-rust`，适用于 Windows、macOS 和带桌面环境的 Linux。
- VPS WebUI 版：`ipfs-node-server`，后端运行在 Linux VPS，通过浏览器管理。
- VPS 终端版：`ipfs-node-terminal`，通过 SSH 和命令行管理。

VPS WebUI 版和终端版不是两个独立项目，它们与桌面版共享 Rust 业务逻辑、Iroh 节点数据、Kubo 配置、内容索引、下载目录和 Relay 设置。

> 推荐使用 Node.js 20 LTS、Rust stable 和 Git。首次安装前端依赖建议使用 `npm ci`。`npm run dev` 只启动网页前端，不能代替完整的 Tauri 桌面后端或 VPS 后端。

## 一、获取项目

如果还没有项目：

```bash
git clone -b prod --single-branch https://github.com/xuanxi369/IPFS-Desktop-Rust.git
cd IPFS-Desktop-Rust
npm ci
```

如果已经有项目，只需进入项目根目录：

```bash
cd IPFS-Desktop-Rust
```

## 二、Windows 10/11 桌面版

### 1. 安装开发环境

以管理员身份打开 PowerShell：

```powershell
winget install --id Git.Git -e
winget install --id OpenJS.NodeJS.LTS -e
winget install --id Rustlang.Rustup -e
winget install --id Microsoft.VisualStudio.2022.BuildTools -e --override "--wait --passive --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
```

如果系统缺少 WebView2：

```powershell
winget install --id Microsoft.EdgeWebView2Runtime -e
```

重新打开 PowerShell 后执行：

```powershell
rustup default stable-msvc
```

### 2. 安装依赖与准备 Kubo

```powershell
cd D:\IPFS-Desktop-Rust
npm ci
npm run setup:kubo
```

`setup:kubo` 是 Windows PowerShell 脚本，用于准备项目使用的 Kubo sidecar。

### 3. 启动桌面开发版

```powershell
npm run tauri dev
```

当前 Cargo 默认桌面入口是 `ipfs-desktop-rust`。如果出现“could not determine which binary to run”，请确认 `src-tauri/Cargo.toml` 中存在：

```toml
default-run = "ipfs-desktop-rust"
```

### 4. 构建安装包

```powershell
npm run tauri build
```

构建结果通常位于：

```text
src-tauri\target\release\bundle\
```

## 三、macOS 桌面版

### 1. 安装开发环境

```bash
xcode-select --install
```

安装 Homebrew 后：

```bash
brew install git node@20
brew link --overwrite node@20
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
source "$HOME/.cargo/env"
rustup default stable
```

### 2. 启动或构建

```bash
cd IPFS-Desktop-Rust
npm ci
npm run tauri dev
```

构建 `.app` 和 `.dmg`：

```bash
npm run tauri build
```

Windows 专用的 `npm run setup:kubo` 不能在 macOS 使用。需要 IPFS 兼容模式时，请单独安装 Kubo，并确保 `ipfs` 位于 `PATH`，或设置：

```bash
export IPFS_GO_EXEC="/实际路径/ipfs"
```

只使用 Iroh 功能时，可以不启动 Kubo。

## 四、带桌面环境的 Linux

以下示例适用于 Ubuntu 22.04/24.04 或 Debian 12：

```bash
sudo apt update
sudo apt install -y \
  git curl wget file build-essential \
  libwebkit2gtk-4.1-dev \
  libayatana-appindicator3-dev \
  librsvg2-dev libxdo-dev libssl-dev patchelf \
  libgtk-3-dev libjavascriptcoregtk-4.1-dev \
  libsoup-3.0-dev libdbus-1-dev libsecret-1-dev
```

安装 Node.js 20 和 Rust：

```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
source "$HOME/.bashrc"
nvm install 20
nvm use 20

curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
source "$HOME/.cargo/env"
rustup default stable
```

启动桌面版：

```bash
cd IPFS-Desktop-Rust
npm ci
npm run tauri dev
```

构建 Linux 安装包：

```bash
npm run tauri build
```

Linux 需要 Kubo 时，请单独安装官方 Kubo，并确保 `ipfs version` 能正常执行；也可以通过 `IPFS_GO_EXEC` 指定可执行文件。

## 五、Linux VPS 通用准备

VPS 不需要安装桌面环境。当前项目提供“终端版”和“WebUI 版”两个入口，可以按需求分别构建和运行。

> 当前 Rust 库仍包含 Tauri 依赖，因此从源码构建 VPS 二进制时，也建议安装上文 Linux 依赖。正式发布时可在构建机完成编译，再将二进制和 Web 静态文件复制到 VPS。

在项目根目录安装依赖并构建 WebUI：

```bash
npm ci
npm run build
```

分别构建两个 VPS 二进制：

```bash
cargo build --release --manifest-path src-tauri/Cargo.toml \
  --no-default-features --features terminal-cli \
  --bin ipfs-node-terminal

cargo build --release --manifest-path src-tauri/Cargo.toml \
  --no-default-features --features headless-server \
  --bin ipfs-node-server
```

生成位置：

```text
src-tauri/target/release/ipfs-node-terminal
src-tauri/target/release/ipfs-node-server
```

可安装到系统路径：

```bash
sudo install -m 0755 src-tauri/target/release/ipfs-node-terminal /usr/local/bin/
sudo install -m 0755 src-tauri/target/release/ipfs-node-server /usr/local/bin/
```

## 六、VPS 终端版

### 1. 交互式启动

```bash
ipfs-node-terminal
```

进入后输入 `help` 查看完整命令，输入 `quit` 或 `exit` 退出。

常用命令：

```text
status                         查看 Kubo 状态
start                          启动 Kubo，并在前台持续监控
stop                           停止当前终端会话启动的 Kubo
restart                        重启 Kubo
node                           查看 Iroh 节点身份
network                        检查 UDP、NAT 和 Relay
relay                          查看 Relay 配置
relay <https-url> [...]        设置自建或区域 Relay
relay --reset                  恢复默认公共 Relay
lan-discovery on|off           开关局域网发现
hardlink-export on|off         开关实验性硬链接导出
list                           查看 Iroh 内容
trash                          查看回收站
remove <cid>                   移入回收站
restore <cid>                  从回收站恢复
add <file>                     上传单文件
add-dir <directory>            上传目录并生成 Ticket
share <cid>                    为本地内容生成 Ticket
fetch <ticket> [file]          下载单文件
fetch-dir <ticket> [parent]    下载目录
mode                           查看使用模式
mode local|compatible|mirror   修改使用模式
storage                        查看节点数据和下载位置
migrate-storage <directory>    安全迁移 Iroh 节点数据
downloads <directory>          设置默认下载目录
downloads --ask on|off         设置下载时是否询问位置
downloads --reset              恢复系统默认下载目录
```

包含空格的路径请使用双引号：

```text
ipfs-node> add-dir "/srv/shared files"
```

### 2. 单条命令模式

```bash
ipfs-node-terminal status
ipfs-node-terminal node
ipfs-node-terminal network
ipfs-node-terminal add /srv/files/example.bin
ipfs-node-terminal fetch 'IROH_TICKET' /srv/downloads/example.bin
```

`start` 会在前台持续运行，按 `Ctrl+C` 正常停止。不要让终端版与 WebUI 同时对同一数据执行删除、迁移或恢复操作。

## 七、VPS WebUI 版

### 1. 本机回环地址启动（推荐）

生成至少 32 个字符的随机令牌：

```bash
export IPFS_NODE_TOKEN="$(openssl rand -hex 32)"
export IPFS_NODE_LISTEN="127.0.0.1:8787"
export IPFS_NODE_WEB_ROOT="$PWD/dist"
ipfs-node-server
```

也可以直接从源码运行：

```bash
IPFS_NODE_TOKEN="$(openssl rand -hex 32)" \
IPFS_NODE_LISTEN="127.0.0.1:8787" \
IPFS_NODE_WEB_ROOT="$PWD/dist" \
cargo run --release --manifest-path src-tauri/Cargo.toml \
  --no-default-features --features headless-server \
  --bin ipfs-node-server
```

默认地址是 `http://127.0.0.1:8787`。浏览器登录时使用启动服务所配置的令牌。

### 2. 通过 SSH 隧道访问（最安全、最推荐）

在自己的电脑执行：

```bash
ssh -L 8787:127.0.0.1:8787 用户名@VPS_IP
```

然后在本机浏览器打开：

```text
http://127.0.0.1:8787
```

这种方式不需要把管理端口直接暴露到公网。

### 3. 公网访问

推荐使用 Caddy 或 Nginx 提供 HTTPS，并反向代理到：

```text
127.0.0.1:8787
```

不要直接暴露 Kubo 的 `5001` 管理 API。也不建议直接把 WebUI 监听到 `0.0.0.0`。

如果确实需要非回环监听，必须同时显式设置：

```bash
export IPFS_NODE_LISTEN="0.0.0.0:8787"
export IPFS_NODE_ALLOW_REMOTE="1"
```

该方式必须配合 HTTPS、防火墙、访问控制和强随机令牌。

### 4. WebUI 环境变量

```text
IPFS_NODE_TOKEN                必填，至少 32 个字符
IPFS_NODE_LISTEN               默认 127.0.0.1:8787
IPFS_NODE_WEB_ROOT             React dist 目录
IPFS_NODE_ALLOW_REMOTE         非回环监听时必须设为 1
IPFS_NODE_MAX_UPLOAD_BYTES     浏览器上传大小上限
IPFS_NODE_MAX_DOWNLOAD_BYTES   浏览器下载大小上限
```

默认单文件上传和下载上限为 2 GiB。需要传输更大文件时，应调整服务端限制以及 Caddy/Nginx 的请求限制。

## 八、VPS 使用 systemd 常驻 WebUI

创建服务用户和目录：

```bash
sudo useradd --system --home /var/lib/ipfs-node --shell /usr/sbin/nologin ipfs-node
sudo install -d -o ipfs-node -g ipfs-node /var/lib/ipfs-node
sudo install -d /usr/lib/ipfs-node/web
sudo cp -a dist/. /usr/lib/ipfs-node/web/
sudo cp scripts/ipfs-node-server.service /etc/systemd/system/
```

创建 `/etc/ipfs-node-server.env`：

```text
IPFS_NODE_TOKEN=替换为至少32个字符的强随机令牌
IPFS_NODE_LISTEN=127.0.0.1:8787
```

保护配置并启动：

```bash
sudo chown root:root /etc/ipfs-node-server.env
sudo chmod 600 /etc/ipfs-node-server.env
sudo systemctl daemon-reload
sudo systemctl enable --now ipfs-node-server
sudo systemctl status ipfs-node-server
```

查看日志：

```bash
journalctl -u ipfs-node-server -f
```

## 九、Iroh、Relay 与中国大陆网络说明

- 局域网发现默认开启，同一局域网优先尝试直接传输。
- 下载进度会显示“局域网直连”“公网直连”或“Relay”。
- `network` 命令和 WebUI 网络诊断可查看 UDP、NAT、直连地址及 Relay 延迟。
- 公共 Iroh 地址发现或 Relay 在部分网络中可能不可达，可以在桌面、WebUI 或终端统一配置自建/区域 Relay。
- 生成 Ticket 前程序会等待有效直连地址或 Relay 地址；如果仍失败，请先运行网络诊断。
- 实验性硬链接导出默认关闭。开启后不要修改导出的文件，否则可能影响 Iroh 内部 Blob；不支持硬链接时会自动回退到安全复制。

## 十、开发与自检命令

前端检查：

```bash
npm run typecheck
npm test
npm run build
```

Rust 检查和测试：

```bash
cargo check --manifest-path src-tauri/Cargo.toml --all-targets --all-features
cargo test --manifest-path src-tauri/Cargo.toml --all-features --all-targets
cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
```

桌面开发版：

```bash
npm run tauri dev
```

注意：

- `npm run dev` 只启动 Vite 页面，没有 Tauri IPC 后端。
- `npm run tauri dev` 启动完整桌面应用。
- VPS 必须运行 `ipfs-node-server` 或 `ipfs-node-terminal`，不能只运行 Vite。
- 未明确要求提交前，不要执行 `git commit`。


